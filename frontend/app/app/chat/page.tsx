/**
 * /app/chat — AI Assistant route inside the /app/* application shell.
 * Re-exports existing ChatPage preserving full SSE streaming, hooks, and state.
 */

export { default } from "@/app/chat/page";
